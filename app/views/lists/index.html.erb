<h2>Lists Index</h2>


<ol>
<% @lists.each do |list| %>
    <li><%= list.name %></li>
    <div class="nav">
      <%= link_to 'Edit', edit_list_path(list) %> | <%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?'} %>
    </div>
  </ol>
<% end %>
